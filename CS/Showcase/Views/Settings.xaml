<Page
    x:Class="Showcase.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Showcase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10,10,10,10">
            <TextBlock Text="News" FontSize="24" Margin="15,15,15,15" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Region" />
                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{x:Bind NewsRegions}" SelectionChanged="RegionCombo_SelectionChanged" Margin="15,0,0,2" />
                <TextBlock Name="NewsCategoriesTextBlock" Grid.Row="1" Grid.Column="0" Text="Category" />
                <ComboBox Name="NewsCategoriesCombo" Grid.Row="1" Grid.Column="1" ItemsSource="{x:Bind NewsCategories}" Margin="15,0,0,2" />
            </Grid>
        </StackPanel>
    </Grid>
</Page>
